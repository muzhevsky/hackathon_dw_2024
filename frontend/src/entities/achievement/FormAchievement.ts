export interface FormAchievement{
    nameEvent: string;
    participant: string;
    dateEvent: string;
    place: string;
}

export interface DataAchievementFromBack{
    date: string;
    id: number | string;
    result: string;
    status: string;
    title: string;
}